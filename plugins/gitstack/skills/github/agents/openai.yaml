interface:
  display_name: "GitHub"
  short_description: "Use `git` and `gh` first for mixed GitHub work, and fall back to shared `ghflow` helpers for higher-level CI, review, stars, or publish flows."
  brand_color: "#1F9D55"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use the bundled GitHub skill to ensure both `git` and `gh` are installed, prefer direct `gh` commands for straightforward GitHub work, and use shared `ghflow` helpers only for failing-PR CI inspection, publish-context inspection, review-thread routing, authenticated-user stars or star lists, or other higher-level shared behavior."
