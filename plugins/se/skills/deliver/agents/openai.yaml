interface:
  display_name: "🚚 Deliver"
  short_description: "Orchestrate workers from scoped work to ready PRs."
  default_prompt: "Use $se:deliver to create or reuse App worker tasks or CLI subagents and deliver this scope through validated, ready PRs."
policy:
  allow_implicit_invocation: true
