interface:
  display_name: "📚 Feature Specification"
  short_description: "Create specs and offer delivery authorization."
  brand_color: "#BE185D"
  default_prompt: "Use $se:spec to save a feature spec and task plan to GitHub or Markdown, then ask whether to authorize delivery to ready PRs."
policy:
  allow_implicit_invocation: false
