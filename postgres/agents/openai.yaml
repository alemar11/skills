interface:
  display_name: "Postgres"
  short_description: "Connect to Postgres, run queries/diagnostics, and lookup official docs."
  brand_color: "#336791"
  default_prompt: "Connect to the target Postgres database, run the requested query or operational check, and summarize results."
